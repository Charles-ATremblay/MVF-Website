import styled from 'styled-components';

const Home = () => {
    return (
        <Container>
        <h1>Home</h1>
        </Container>
    );
}

const Container = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
`;

export default Home;
